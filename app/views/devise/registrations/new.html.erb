<div class="row">
  <div class="col m6 offset-m3 center">
    <h5>Sign up</h5>
    
    <%= link_to "Sign Up with Google", user_google_oauth2_omniauth_authorize_path, class: "btn red darken-1" %>
    <br/><br/>
    <%= link_to "Sign Up with Facebook", user_facebook_omniauth_authorize_path, class: "btn blue darken-4" %>
    <br/><br/>
    <%= link_to "Sign Up with Github", user_github_omniauth_authorize_path, class: "btn grey darken-2" %>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      
      <% if resource.errors.any? %>
        <% resource.errors.full_messages.each do |error| %>
          <%= js add_gritter(error, title: "Error:", sticky: false) %>
        <% end %>
      <% end %>
      
      <div class="input-field">
        <%= f.label :name %><br />
        <%= f.text_field :name, autofocus: true, class: "border-bottom" %>
      </div>
    
      <div class="input-field">
        <%= f.label :email %><br />
        <%= f.email_field :email, class: "border-bottom" %>
      </div>
    
      <div class="input-field">
        <%= f.label :password %>
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "off", class: "border-bottom" %>
      </div>
    
      <div class="input-field">
        <%= f.label :password_confirmation %><br />
        <%= f.password_field :password_confirmation, autocomplete: "off", class: "border-bottom" %>
      </div>
    
      <button class="btn waves-effect waves-light" type="submit" name="action">Submit
        <i class="material-icons right">send</i>
      </button>
    <% end %>
  </div>
</div>
